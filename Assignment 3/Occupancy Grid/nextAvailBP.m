function nextAvailBP(boardType, board)

    switch boardType
        case 'main'
           
        case 'P1'

        case 'P2'

        otherwise
            fprintf('FROM BPtoCoord: %s is not a valid board type\n', boardType);
    end
        
end